# 研究进度记录 · vggt_yoloe 土方量测

> 目标：把"VGGT + YOLOe 挖掘机三维重建/地形量测"做成一篇论文/毕业课题。
> 完整图文总结见 `research_summary_20260611.html`。

---

## 已完成（截至 2026-06-11）

### 研究定位
- 确定方向：**单目无人机土方（挖填）量测**；贡献必须做在 **VGGT 之上**（VGGT 输出无尺度、单次前馈、不管量测）。
- 三贡献框架：① 语义尺度锚定 ② 抗光晕鲁棒量测（核心）③ ExcaVol 基准。
- "工作量"问题的答案：贡献与 backbone 正交（可在 DA3 复验）、每模块对 raw-VGGT 报 Δ、发现+量化+纠正一个失效模式。

### 关键发现链（现象 → 机理）
1. **语义 mask 锚定** 把尺度方差从 83%（naive 两端点）压到 2–5%（↓15–40×）。
2. **"竖直压扁"是假象**（一度误判，已修正）：置信度扫描显示竖直 Y 几乎不变，是低置信点把**水平 footprint** 吹大、拉低了 高/宽 比。
3. **真机理 = 深度不连续处的"水平光晕"**：物体/坑沿边界像素深度不可靠，沿视线甩到地面 → 单向水平裙边（Z 向 footprint 被吹大 3.5×）。三重确认：光晕点深度梯度 2.5×、corr(conf,∇depth)=−0.32、可视化。
4. 推论：**体积相对误差 ≈ 3 × 尺度相对误差**；naive DEM/方量会被光晕系统性污染。

### 产出的代码（均可复用，repo 根目录）
- `scale_calibration.py` — 尺度标定 harness（robust 像素→3D、已知尺寸物求 scale、一致性、敏感度、CLI、网格帧/picker）
- `vertical_fidelity_study.py` — 物体 aspect 探针（YOLOe + 重力对齐，多场景）
- `diagnose_flattening.py` — 根因诊断（pointmap vs depth 头、相机系分解、置信度分层）
- `viz_halo.py` — 光晕可视化（图像空间 + 3D 俯视 + 深度梯度相关）
- `build_summary.py` — 生成 HTML 总结
- 结果：各 workspace 下 `scale_result.json` / `vertical_fidelity_results.json` / `halo_viz*.png`

### 环境备忘
- numpy/几何分析：conda env `vggt`；YOLOe 分割：env `yoloe`，需从 `WS/yoloe` 目录跑（依赖 `mobileclip_blt.pt`），权重 HF 缓存 `jameslahm/yoloe`。
- 主测试场景：`workspaces/session_20260611_162643_869764`（挖掘机+人+坑，斜视 66°）。
- predictions.npz: world_points (S,294,518,3), world_points_conf, world_points_from_depth, depth, extrinsic, intrinsic, images。

---

## 2026-06-12 新增（竖直压缩 & 多帧不恢复）

> 代码：`vertical_recovery_study.py`；结果：`pit_judgment.png` / `vertical_recovery.png` / `vertical_recovery_result.json`（均在主测试 workspace 下）。

### 重要转折：主测试场景**没有可测的坑**，主线是"竖直压缩"
- 原计划"框右下角坑算方量"在这帧**不成立**：全场高程起伏仅 **0.39m**；候选 ROI（u300–518,v150–294）中位"低于地面深度"= **−0.01m**（即不凹陷），>0.2m 的坑像素 = 0。**不能硬造坑体积数字**。
- 我判断坑的方法（已出图 `pit_judgment.png`）：拟合参考地平面 → 逐像素 `depression = ground_Y − Y`（正=凹成坑，负=高出地面）。该图 C 面板红色才是坑，本场景全是近 0/蓝（物体高出），无红 → 无坑。

### 三条硬结论（挖掘机探针，真高 3.0m，provisional）
1. **竖直压缩 ≈ 7.9×**：挖掘机重建高度仅 **0.38m**（vs 真 3.0m）。重力方向三法交叉验证到 **0.2°**，排除对齐 bug。
2. **两个预测头一致压缩**：pointmap 头 0.378m、depth 头 0.372m → **depth 再三角化/重投影救不回竖直**，压缩在 VGGT 预测本身。
3. **多帧聚合不恢复高度**：各帧顶高 0.01–0.41m，融合后 p95=0.33m / p99.5=0.40m = **仅等于最好单帧**；但覆盖率 **↑16×**（4135→66636 点）。→ 多帧只能补洞/补覆盖，**治不了竖直压缩**。

### 与昨天"修正"的关系（已对齐，非矛盾）
- 昨天"竖直 Y 几乎不变、光晕只吹大水平 footprint"在**置信滤波清裙边**这一层成立（Z 1.08→0.17m）。
- 但**绝对竖直**始终被压 ~8×（连最干净核心也只有 0.35m）——这是**叠加在光晕之上的第二个失效模式**，去光晕治不了。两者并存。

---

## 下一次可以做

### 第一优先：竖直压缩的纠正方法（贡献②升级为核心）

**E0 + E1a 已完成（2026-06-12 当天）**，代码 `vertical_gauge_test.py` / `vertical_correct_e1a.py`，结果同名 png/json：
- **E0 根因定性**：压缩是**深度无关的仿射 gauge**——挖掘机近/远两半 k=7.93 vs 7.96（差 0.4%）→ 不是随深度的投影；人主轴竖直(偏3°)只是被压短 → 是缩放不是"放倒"（推翻了早先用噪声端点的"沿视线放倒"猜测）。跨物体 k 有差异（挖7.9 / 人10.8）。
  → **重大收窄**：纠正只需**低自由度全局仿射**，**不必**训练逐像素稠密矫正场，"训大模型"必要性大降。
- **E1a 单标量纠正**：绕地面拉伸 `Y'=ground+k·(Y−ground)`。挖掘机标定→人残差 −27%；人标定→挖掘机 +36%；k 均值(9.4) 两者 ±18%。→ 单标量纠数量级但留 ~15–30% 残差。
- 文献根因支撑：VGGT-SLAM(2505.12549) 15-DOF 投影/stretch 歧义；航空块评测(2507.14798) 小基线失效区；AMB3R(2511.20343) 冻结特征 scale head。

**E1b 已完成（2026-06-12）**，代码 `vertical_correct_e1b.py`：对比三种纠正模型——M0 单标量(残差36%)、M1 过原点比例 h=0.118H(残差27%)、M2 带偏置仿射 h=0.170H−0.132(2点恰好拟合)。
- 发现"小物体压更狠"(挖k=7.9/人k=10.8)，M2 偏置模型(固定高度损失~13cm)可解释。
- 2 控制物分不清 M1/M2：模型在控制物附近(表观0.16–0.38m)重合、外推即分叉（表观0.05m处 M0给0.40m vs M2给1.07m，差2.7×）。
- **精确化的标定杆要求**：坑深通常落在外推区，故标定杆须**高度跨度大（矮/中/高 3–4 种），覆盖目标量程**。

**Q2 已完成（2026-06-12）**，代码 `q2_signature_stability.py`（复用 `vertical_fidelity_results.json` 18条/10场景，零新计算）：
- aspect 缺口(canonical/measured = k_v/k_h，尺度无关) 在挖掘机上跨 10 场景 **2.248±0.042，CV 仅 1.9%** → 畸变是**系统性可复现**的 VGGT 属性；人 CV 55%(小目标噪声)。
- **判定：方向二（预标定/学小纠正器）可行**——纠正对象稳定，不必每飞行重标。
- caveat：deficit 混了光晕宽度膨胀；证"绝对 k_v 跨场景稳定"还需每场景尺度。

**Q2b 已完成（2026-06-12）**，代码 `q2b_vertical_ratio.py`：尺度无关的跨物体竖直比 `R=挖掘机_Y/人_Y`（理想 1.76）在人 mask≥350px 的 5 个干净场景里 **R=3.00，CV 仅 1.1%** → 比 aspect 更干净地坐实"竖直畸变物体层面也稳定可复现"。R/理想=1.70 → **人比挖掘机多压 1.70×**（印证 E1b 小物体压更狠）。
- **可识别性结论**：绝对 k_v 无法只用竖直已知量单场景标定（尺度与压缩纠缠），须外部水平基准/遥测（=大坑标定杆要补的）。

**全部内容已整理进**：`experiment_showcase.html`（科研初稿，12 图，新增 5.5/5.6/5.7 = E0/E1a/E1b/Q2/Q2b，含大白话+读图指南）。

**下一步：**
- [ ] **E1b-full**：等大坑标定杆(多高度，覆盖量程)到位后，过定拟合并验证 M1/M2，钉死残差。
- [ ] **方向二原型**：仿 AMB3R 在冻结 VGGT 特征上训小头，预测竖直纠正参数（Q2 已证可行）。
- [ ] **backbone 复验**：DA3/DUSt3R 上跑同样探针，确认是 VGGT 特有还是单目前馈通病。
- [ ] **大坑实验硬清单**：坑周围撒 **3–4 根已知高度标定杆**（不同位置/深度）过定仿射 + 跨物体验证。
- [ ] 方向二(可选/往后)：仿 AMB3R 在冻结 VGGT 特征上训小头，**仅为无控制物时预测仿射参数**（便利非刚需）。
- [ ] 方向一(遥测，若可得)：DJI GPS/IMU/云台 → 绝对尺度+重力 → 度量 BA/重三角化。（当前仓库无 SRT，遥测拿不到）

### 保留：把账算到"真实的坑"上（需换场景）
- [ ] 当前主场景无坑。**在其余 workspace（尤其 05-22 dji_fly 组）找有明显土堆/坑、竖直落差更大的场景**，复用 `pit_judgment.png` 的 depression 判据先验证"有没有坑"，再算方量。
- [ ] 即便找到坑，方量仍会被 ~8× 竖直压缩污染 → 必须先有上面的竖直纠正。

### 第二优先：抗光晕滤波方法（贡献②的方法本体）
- [ ] 设计去光晕滤波：空间一致性/法向一致性/置信度联合，**去裙边同时保建面点密度**。
- [ ] 对比 baseline：钝置信度百分位阈值 vs 提出的方法（覆盖度 vs 干净度权衡曲线）。

### 第三优先：坐实结论
- [ ] **DA3 复验**：在 Depth-Anything-3 上跑同样探针，确认光晕是 VGGT 特有还是单目通病（决定贡献表述）。
- [ ] **可控实拍 GT**：放已知尺寸标定物 + 已知方量，把名义 canonical 换成真值，钉死尺度与体积误差。
- [ ] 补不同机型/不同航线场景，扩大 distinct-scene 样本。

### 写作
- [ ] 趁热把"问题—贡献—方法—实验—时间线"写成开题/intro 初稿（链条已完整）。

---

## 注意 / 坑
- 绝对尺度目前用临时人身高 1.70m，**所有绝对米制数字是 provisional**，比例类结论才是硬的。
- 小物体（人）置信滤波只能部分恢复 aspect，可能叠加真实欠重建；**用大挖掘机做干净探针**。
- `vertical_fidelity_study` 里同一台挖掘机的多个 workspace 是同视频重跑，distinct 样本数被高估，需去重看待。
