# VGGT × YOLOe：近一周项目进展

> 时间范围：2026-07-21 至 2026-07-28  
> 主题：从离线三维重建到可信实时地形建图

## 1. 汇报概览

本周工作的核心，是在既有的“上传视频或图像后进行离线重建”管线旁，增加一条可由真实相机持续驱动、可质量控制、可持久化恢复的在线地形建图路径。

项目现在由两条路径构成：

1. **既有离线管线**：视频/图像上传 → YOLOe 语义分割（可选）→ VGGT 重建 → 重力对齐 → DEM 构建 → 高程查看与点云编辑。
2. **本周新增在线管线**：真实相机/视频流 → 关键帧选择 → 滑动窗口 VGGT → 坐标配准与变化判断 → 全局 DEM 融合 → Tile 发布与实时查看。

新增能力以 `streaming/` 为边界进行集成。原来的 `/reconstruct`、`/fit_elevation`、YOLOe 服务和上传工作流保持协议不变。

## 2. 系统总逻辑

```text
                     ┌──────────────── 既有离线路径 ────────────────┐
视频 / 图像上传 ────► YOLOe 语义分割（可选） ───► VGGT 三维重建
                                                    │
                                                    ▼
                                  重力对齐 → 地面筛选 → DEM 插值
                                                    │
                                                    ▼
                              Three.js 高程查看 / 点云编辑 / 体积估算
                     └──────────────────────────────────────────────┘

                     ┌──────────────── 本周新增在线路径 ────────────┐
USB / RTSP / HTTP ──► 关键帧筛选与滑窗缓存 ────► VGGT 局部重建
                                                    │
                                                    ▼
                           世界锚定 → 局部配准 → 全局 DEM 融合
                                                    │
                                                    ▼
                            质量门控 → Tile 发布 → 实时地形查看器
                     └──────────────────────────────────────────────┘
```

两条路径共享关键的三维空间表达：VGGT 点云、重力对齐坐标系、地面点选择和规则 DEM 插值。在线路径在此基础上额外解决了实时输入、窗口调度、跨窗口坐标一致性、质量门控、持久化和增量发布的问题。

## 3. 在线建图的端到端过程

### 3.1 连续视频帧采集

输入源包括本地视频、USB/V4L2 相机、RTSP 相机流、HTTP/MJPEG 流。`streaming/camera_source.py` 负责持续解码，`streaming/source_factory.py` 统一构建输入源。

实现要点：

- 支持 USB、RTSP、HTTP/MJPEG；IP Webcam 根地址可自动补全到 `/video`。
- 以目标 FPS 进行采样，避免把每一帧都送入后续计算。
- 设置打开超时和读取超时，并维护自动断线重连逻辑。
- 对含用户名和密码的连接地址进行脱敏，避免诊断信息泄露凭据。
- 采用“最新帧覆盖”策略：重建速度低于相机帧率时，新帧覆盖旧帧，而非无限排队。

该策略的关键价值是控制端到端延迟。VGGT 推理较慢时，如果积压所有帧，系统会逐渐处理历史画面；只保留最新帧则保证重建始终尽可能反映当前场景。

相关接口：

- `/stream/start`：启动取流或滑窗重建。
- `/stream/frame.jpg`：获取服务端保存的最新帧预览。
- `/stream/status`：获取连接状态、FPS、帧龄、重连和重建状态。

### 3.2 有效关键帧筛选

原始视频中相邻帧往往高度相似。若对每个连续帧执行 VGGT，不仅浪费 GPU，也可能对同一场景重复重建。因此加入 `streaming/keyframe_buffer.py` 中的 `KeyframeBuffer`。

实现要点：

- 对输入帧生成轻量级帧签名，估计当前帧与已保留帧的相似度。
- 可使用 ORB 特征进一步辅助判断，使“保留 / 跳过”的策略接近离线流程。
- 使用固定容量缓存维护关键帧滑动窗口；新信息进入时淘汰旧信息。
- 只有关键帧数量达到 `min_frames`、并且当前窗口相比上次执行确实存在新增帧时，才触发一次重建。
- `interval` 控制重建调度间隔，`capacity` 控制窗口上限，`sim_thresh` 控制相似帧跳过阈值。

由此，推理开销随场景信息增量而不是随原始视频帧率增长。该部分是在线方案获得可用实时性的第一道优化。

### 3.3 局部三维重建

`streaming/reconstruct_loop.py` 中的 `ReconstructLoop` 负责把关键帧窗口转换成一次局部重建结果。

过程如下：

1. 从关键帧缓存读取当前窗口。
2. 将窗口帧写入当前工作目录，复用既有 VGGT 推理路径。
3. 调用 VGGT，获得相机位姿、深度信息与点云。
4. 对点云执行重力对齐和地面点选择，构建局部 DEM。
5. 将结果组织成 `DemResult`，包含高程、有效掩码、点云、锚定信息和诊断数据。

`streaming/pipeline.py` 封装了从帧集合到 DEM 的公共流程，并定义 `DemResult` 与统一消息转换函数。这使流式 DEM 与离线高程查看器的 DEM 使用相同的空间表达。

资源管理方面，`vggt_service.py` 让在线和离线推理共用 VGGT 模型互斥锁，避免两个请求同时占用 GPU 并影响原有服务稳定性。

### 3.4 坐标对齐与质量判断

不同滑动窗口独立重建时，局部坐标系可能不一致。若直接合并不同窗口的 DEM，地图会抖动、漂移，甚至把不同地点错误叠加。因此在线路径使用“首轮锚定、后续配准”的方式维持坐标稳定。

实现要点：

- 首轮可信重建建立 `Anchor`，作为后续观测的世界坐标参考。
- 该锚点可冻结，防止后续局部窗口改变全局坐标方向或尺度。
- 后续 `DemResult` 先与基准地图进行注册/配准，再进入 DEM 融合。
- 点云经过重力对齐后，Y 轴被解释为高程；在 X-Z 平面上选择地面候选点并插值为规则网格。

在融合前必须进行质量门控。增量重建至少检查以下指标：

- 配准误差（RMSE）是否低于允许上限。
- 当前窗口与基准地图的覆盖率是否足够。
- 检测到的变化面积比例是否合理。
- 高程变化是否满足物理范围约束。
- 是否存在足以证明“真实变化”的邻域支持。

若任一关键条件不通过，则拒绝该轮更新、记录拒绝原因、保持当前 `map_version` 不变，也不发布新的地形 Tile。

### 3.5 可信 DEM 更新与发布

通过配准和质量门控的局部 DEM 才会进入 `streaming/global_dem.py` 中的 `GlobalDem`。

实现要点：

- 在固定世界范围内维护全局高程栅格，而非只保存当前窗口。
- 使用融合配置控制世界尺寸、Tile 尺寸、衰减、变化阈值和自动扩展范围等行为。
- 把局部观测投影到全局栅格，更新有效区域；仅产生变化的 Tile。
- `streaming/elevation_publisher.py` 把结果序列化为高程消息和 Tile JSON，可选发布到 MQTT。
- 流接口保留最近一次地形快照，供实时查看器查询。

输出对象包括当前 DEM、全局 DEM、变更 Tile、地图版本号及相关诊断数据。外部系统和地形查看器可只消费新增或变化的 Tile，避免反复传输完整地图。

## 4. 两阶段 MapSession：把在线验证变成正式地图

默认滑窗可以验证“相机 → 关键帧 → VGGT → DEM”的技术闭环；正式建图则通过持久化 `MapSession` 明确分为两个阶段。

### 4.1 初始化阶段

初始化阶段受控采集较大关键帧集合，生成：

- 基准点云；
- 基准 DEM；
- 世界坐标锚点；
- 参考帧；
- 初始化质量报告。

`streaming/initialization.py` 对帧数、点数、空间覆盖率和高程跨度等指标进行检查。通过自动检查后，会话进入 `INIT_REVIEW`，仍需人工确认是否接受当前基准地图。

### 4.2 增量更新阶段

确认后，会话进入 `READY`。新的小窗口重建结果会被定位到基准地图坐标系，经过变化检测后，仅将可信变化融合到地图。

`streaming/change_detection.py` 负责：

- 评估配准 RMSE；
- 计算覆盖率；
- 评估变化面积比例；
- 约束高程变化的物理合理性；
- 保存拒绝原因；
- 融合通过门控的 DEM。

`streaming/two_stage_loop.py` 将以上策略组合成正式流程：初始化只执行一次受控批量重建；增量阶段只融合、持久化和发布已通过门控的结果。

### 4.3 会话状态机

```text
IDLE
  ↓
INITIALIZING
  ↓
INIT_REVIEW ──（人工拒绝）──► REINIT_REQUIRED
  │
  └──（人工通过）──► READY ──► UPDATING ──► READY
                                      │
                                      ├──► DEGRADED ──► UPDATING / READY
                                      │
                                      └──► REINIT_REQUIRED
```

状态含义：

- `IDLE`：会话已创建但尚未开始。
- `INITIALIZING`：正在建立基准地图。
- `INIT_REVIEW`：自动质量检查完成，等待人工审核。
- `READY`：基准地图可信，可接受增量更新。
- `UPDATING`：正在处理新的滑动窗口。
- `DEGRADED`：存在质量异常但可尝试受控恢复。
- `REINIT_REQUIRED`：连续拒绝或严重异常，需要重新初始化。

### 4.4 持久化与故障恢复

`streaming/map_session.py` 的 `MapSession` 保存版本化会话目录，核心内容包括：

- `manifest.json`：会话状态、地图版本、质量报告、变更报告、人工审核结果与配置。
- `anchor.npz`：世界坐标锚点。
- `current_dem.npz`：当前 DEM。
- `global_dem.npz`：全局融合 DEM。
- `reference_frames/`：参考帧。

JSON 与 NPZ 文件均通过“写临时文件后原子替换”的方式保存。服务在写入过程中崩溃时，不会留下半写入的 manifest 或 DEM 文件；重启后也可从该会话目录恢复。

## 5. API 与交互集成

### 5.1 基础实时流接口

- `POST /stream/start`：连接视频、USB、RTSP 或 HTTP/MJPEG；支持仅取流诊断或启动重建循环。
- `POST /stream/stop`：停止循环并释放相机资源。
- `GET /stream/status`：查询连接、FPS、帧龄、重连、关键帧与重建状态。
- `GET /stream/frame.jpg`：获取最新帧。
- `GET /stream/terrain`：获取最近发布的地形快照。
- `GET /stream/pass`：获取最近单次重建的摘要信息。

### 5.2 正式会话接口

- `POST /stream/session/initialize`：创建或加载会话，启动初始化重建。
- `POST /stream/session/finalize`：人工接受或拒绝初始化结果。
- `POST /stream/session/update`：对已确认基准地图执行可信增量更新。
- `GET /stream/session/status`：读取会话状态、版本与质量信息。

### 5.3 Gradio 控制面板

`orchestrator.py` 中增加“实时相机与在线建图”面板，提供以下可视化操作：

- 连接相机并预览最新帧；
- 查看相机与重建健康指标；
- 启动、停止滑动窗口验证；
- 初始化地图；
- 人工通过或拒绝初始化；
- 从已确认会话启动可信增量更新。

因此，建议按“仅取流诊断 → 滑窗闭环验证 → 两阶段正式建图”的方式逐步部署，而不是一开始就让实时输入写入正式地图。

## 6. 本周交付时间线

### 7/21：会话建图基础

- 增加 `MapSession`、初始化策略、变化检测与会话 API。
- 增加相关会话和兼容性测试。

### 7/22–7/24：DEM 产品化与技术沉淀

- 完善高程导出和地形数据格式规范。
- 增加 `elevation_plane.py` 函数说明。
- 更新架构文档、技术报告、README。
- 编写滑动窗口重建流程说明。

### 7/26：在线会话整合

- 接入关键帧缓存。
- 增加 Gradio 会话控制。
- 完成服务端协调与实验性会话产物生成。

### 7/27–7/28：全局 DEM 与实时可视化收口

- 补齐 `GlobalDem`、高程发布器、流端点和实时 Terrain Viewer。
- 增加相机流、MapSession 与地形查看器测试。

## 7. 关键工程保障与当前边界

### 已具备的保障

- **兼容性**：新增会话能力显式通过 `/stream/session/*` 启用，不改变既有离线协议。
- **资源安全**：在线和离线请求共享 VGGT 模型锁，避免并发 GPU 推理影响服务。
- **故障安全**：会话产物原子保存，服务重启后可加载继续。
- **可观测性**：暴露连接、FPS、帧龄、重连与重建状态；支持纯取流诊断。
- **测试覆盖**：包括 MapSession、相机流、实时地形查看器与 API 路由契约。

### 当前边界与下一步关注点

- 已完成 CPU/兼容性测试和 API 契约检查。
- 尚需在真实相机输入下进行长时 GPU 稳定性测试。
- 默认门控阈值仍需针对具体现场尺度、相机运动方式和地形噪声进行标定。
- 需进一步评估快速运动、强遮挡、光照突变、弱纹理地面和大范围尺度变化对配准和 DEM 质量的影响。

## 8. 组会讲述建议

建议采用以下叙事顺序：

1. **问题**：离线重建无法持续反映现场变化；单次结果也没有长期质量约束。
2. **总体方案**：实时滑窗提供快速观测，两阶段 `MapSession` 负责正式地图质量。
3. **关键突破**：最新帧优先、关键帧调度、跨窗口坐标稳定、全局 DEM 融合、质量门控与持久化。
4. **工程结果**：新能力渐进接入，没有破坏离线重建、YOLOe 和既有 UI。
5. **当前边界**：真实场景长期稳定性和阈值标定仍是下一阶段重点。

一句话总结：**项目已从“能生成一次 DEM 的离线重建工具”，扩展为“可接入实时视频、可验证、可恢复、可对外发布的可信地形建图框架”。**
