# 挖掘机姿态检测（大臂/小臂/铲斗骨架）— 进度落盘

> 状态：几何法 baseline 已验证可行 · 2026-06-29
> 工作目录：`workspaces/session_20260629_100116_092814/`
> 目标：从单目深度/点云估计挖机姿态——大臂、小臂、铲斗的骨架/关节位置（4 个语义关键点）。

---

## 0. 任务定义

把挖机工作装置抽象为一条运动学链，求其姿态：
- **大臂(boom)** ≈ 一条直杆
- **小臂(stick/arm)** ≈ 一条直杆
- **铲斗(bucket)** ≈ 一个多边形/凸包
- **4 个语义关键点**：铲斗尖 → 小臂-铲斗关节 → 大臂-小臂肘关节 → 大臂根/底座安装点

输出：每帧 4 个关键点的 2D 像素坐标（配 `world_points` 可抬升为 3D），用于关节角、连杆姿态分析。

---

## 1. 数据来源

- 单帧预测：`predictions.npz`
  - `depth (14,168,518,1)`、`world_points_from_depth (14,168,518,3)`、`images (14,3,168,518)`、`intrinsic`、`extrinsic`
  - 地面是干净平面，挖机是平面上的凸起 → 几何分割很容易
- **GT 数据源 = Unity**（合成数据，关键点真值可零成本导出，见 §4）

---

## 2. 已做：几何法 baseline（无需训练）

脚本（均在 session 目录下）：
- `seg_demo.py` — 整机分割 + 高度图 + 法向图 + 深度边缘
- `pose_demo.py` — **主脚本**：整机分割 → 骨架化 → 最长测地路径 → Douglas-Peucker 折线 → 关节编号
- `pose_demo2.py` — 带「宽度截断」的实验版（截掉钻进车体的折线，并算 3D 关节+大臂-小臂夹角）。**用户决定暂不采用截断**，主线回到 `pose_demo.py`。
- `make_sheet.py` — 14 帧结果拼 contact sheet

### 2.1 整机分割（已验证，效果好）
对 `world_points` 做 RANSAC 平面拟合（地面，法向≈`[0,-0.80,-0.60]`）→ 算每像素离地高度 → 阈值 1cm → mask。
比直接卡深度灰度好：离地高度对相机距离不敏感，零地面误检，~5800 px 干净抠出整机。

### 2.2 骨架折线 + 关节编号
1. mask → `skeletonize` 得 1px 骨架
2. BFS 两次取最长测地路径（铲斗端↔底座端）
3. `approxPolyDP(epsilon=6)` 简化为折线，顶点=候选关节
4. **强制 0 号在铲斗端**：用距离变换(DT)判别——底座端连进宽车体(DT大)，铲斗端是细臂孤立尖(DT小)；两端各取一段均值，DT大的端翻转。`approxPolyDP` 后再对齐一次保证 `poly[0]` 最靠近铲斗端。
5. 铲斗 = 铲斗端半径 22px 内 mask 点的凸包多边形

### 2.3 可视化约定
- 红线=折线(大臂+小臂)、绿点=折线顶点(候选关节)、白色数字=顶点编号(0=铲斗端起)、品红多边形=铲斗、黄=完整骨架(中间产物)
- 输出：`pose_demo/fX_pose.png`、`pose_demo/contact_sheet.png`

---

## 3. 结果与几何法的天花板

14 帧可用率 ≈ **11/14 (79%)**：

| 帧 | 结果 |
|---|---|
| f0–f9, f12 | ✅ 好：0号稳定在铲斗端，折线+多边形贴合，编号语义一致 |
| f13 | ⚠️ 一般：肘关节略偏，需人工微调 |
| f10 | ❌ DT 端点判据被履带骗（铲斗举到车体上方、履带末端更孤立 → 0号判错） |
| f11 | ❌ 臂收回贴/藏进车体，骨架糊掉 → 折线失效（任何几何法无解） |

**几何法固有弱点（调参不可根治）**：
1. `approxPolyDP` 顶点数随形状变(4~9)，**编号无跨帧语义一致性**——「几号=哪个关节」不稳定
2. 端点定向在铲斗非最孤立点时(f10)会错
3. 自遮挡(f11)时 mask 无可用臂骨架

→ 几何法定位：**baseline + 自动初标注器**，不能跨任意姿态鲁棒。

---

## 4. 下一步：学习式关键点检测（方法 D，主路线）

### 推荐模型
- **任务**：4-keypoint 单实例姿态估计（与人体姿态同构，但点少、刚性连杆）
- **模型**：**HRNet-W18**（精度优先）或 MobileNetV3+heatmap（实时），热图回归范式
- **框架**：**MMPose**（环境已有同源 mmseg），COCO-keypoints 格式，1 个 skeleton 定义连杆
- **刚性先验加分项**：
  - 每点 visible/occluded 标志 → 网络学会遮挡帧输出低置信（治 f10/f11）
  - 连杆长度约束 loss（相邻关键点距离≈常数）→ 遮挡时合理外推
  - 时序平滑（视频序列，卡尔曼/1D-TCN）

### 数据：Unity 导出 GT（最大杠杆）
两条路：
- **A. Unity Perception 包**：`KeypointLabeler` + 自定义 `KeypointTemplate`(4点+skeleton)，自动导出 2D像素+遮挡state(0不存在/1遮挡/2可见)+3D，输出 SOLO/COCO JSON，MMPose 近可直接吃。还能顺带导分割/深度/bbox + domain randomization。
- **B. 自写 C# 脚本**：关节 Transform 挂空 GameObject → `cam.WorldToScreenPoint` 取2D + raycast 判遮挡，逐帧写 json。坐标系完全可控，便于和 VGGT 相机约定对齐。

**待确认**：Unity 工程能否改（挂脚本/加 Perception）？偏好官方 Perception 还是自写脚本？

### ⚠️ 坐标对齐坑
VGGT 输入是 518×168 裁切/缩放。Unity 导出的 RGB 和关键点坐标必须经过与 VGGT 相同的 resize/crop，关键点坐标同步变换——在转换脚本里处理。

### 训练管线
```
Unity (Perception/自写) → RGB + 4关键点(2D+3D+visible/occluded) + 相机内外参
  → 转换脚本 → COCO-keypoints JSON (4 kpts, 1 skeleton)
  → MMPose HRNet-W18 训练 (+可见性监督 +连杆长度约束)
  → 推理: 任意帧 → 4个2D关键点+置信度 → 配 world_points 抬成3D姿态
```

---

## 4b. 已做：粗粒度运动状态（L2 使能，不需精确姿态）

> 状态：已验证可行 · 2026-06-29 · 脚本在 `experiments/arm_motion_state/`

用户的中间需求：**先不追求精确关节姿态**，只要铲斗/手臂的粗运动状态（正在抬升 up / 下降 down / 静止 static）+ 速度/加速度，即可补齐 scene graph L2 的 `state[t]`。

### 方法
1. **垂直轴 = 地面 RANSAC 平面法向**（`[0,-0.80,-0.60]`），**不用相机轨迹重力对齐**——该 session 轨迹法把垂直轴估歪了（`run_motion.py` v1 的 bug）。
2. 信号 = 沿地面法向的**离地高度**。`world_points` 在世界系，相机运动天然消除。
3. 代表点 = 整机 mask 中靠铲斗端那批高点（图像左侧 12% 列）的**平均离地高度**，不用帧间乱跳的"最远点/最高点"。
4. 平滑(k=3) → `vZ=gradient(H)/DT` → `aZ=gradient(smooth(vZ))/DT` → 状态机（`eps=max(0.012, median|ΔH|/DT*0.5)`）。

### 可视化（dashboard）
`run_dashboard.py`：单帧叠加分割+铲斗代表点 + 速度箭头(实线,升绿/降红) + 加速度箭头(橙虚线) + 右下文字面板(H/vZ/aZ/state/|v3D|) + 底部高度时间轴+游标。
**箭头方向用物理速度驱动**（竖直分量锁定 = -vZ 符号，屏幕向上为负 y），**不用像素位移**（像素位移会乱跳、与 state 矛盾）。
输出 MP4：`cv2.VideoWriter` mp4v（无 ffmpeg）。

### 结果
- 14 帧版（`arm_motion_state/` 根）：抬升→峰→下降两段式。
- 30 帧版（`run30/`，同段视频 `session_20260629_173356_784627`）：信号更细，完整 rise→peak(f7,H=0.182)→fall→中段平稳→末端小起伏。两版 ground normal 一致(`[0,-0.80,-0.60]`)，互验。
- 产物：`motion_curves.png`、`dashboard/fXX.png`、`dashboard_sheet.png`、`motion_state.mp4`、`motion_state.json`。

### 下一步（用户要求 2026-06-29）
对**手臂上多个关节**（铲斗尖/小臂-铲斗关节/肘/根）各自做同样的速度/加速度计算 → 见 §4c。

---

## 5. 与 Scene Graph 的关联

见 `SCENE_GRAPH_PLAN.md`（v0.3.2 已同步）。姿态检测是 scene graph **L2 施动者层**的 `state` 估计器，三处接点：

1. **L2 `state[t]` 的物理基础**：machine 节点的 `state`(digging/moving/idle) 与铲斗 3D 位置由姿态估计提供；光靠 bbox 质心判不出"在挖"还是"在移动"。
2. **机制 5 dig 事件触发器**：铲斗关键点轨迹（下沉→贴地→抬升）= 一次挖掘动作的最可靠信号。
3. **机制 5 因果定位锚**：铲斗尖关键点 + `world_points` 抬升 → "这一铲改的是地形哪个 (x,y)"，兑现"几何动作因果驱动定位"主张（比图像相似度比对精确）。
4. **L3 `reachable` 边**：可达工作半径由连杆长度+关节角算出，来自姿态输出。

定位：**使能模块(enabler)，非论文主线贡献**——服务于地形核心叙事与更准的 dig 事件因果定位，不另立"挖机姿态估计"贡献点，以免与地形核心抢焦点。
